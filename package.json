{
  "name": "mars-explorer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "postinstall": "cd mars-explorer-client && npm install  &&  cd ../mars-explorer-server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.3.0",
    "cors": "^2.8.1",
    "express": "^4.15.0"
  }
}
